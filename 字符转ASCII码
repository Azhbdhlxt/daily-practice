#include <stdio.h>
int main(int argc[], char *argv[])
{
    char ch;
    printf("Press a key and then press Enter:\n");
    ch = getchar();
    ch = ch + 32;
    printf("%c, %d\n", ch, ch);
    return 0;
}
