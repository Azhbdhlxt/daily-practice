#include <stdio.h>
#include <stdlib.h>
void main()
{  
    FILE *fp;
    
    if ((fp=fopen("mydata.txt","w"))==NULL)
        {
            printf("file open error!\n");
            
            exit(0);
            
        }
    else
	{
		printf("File open is OK!\n");
    
		/*......		/* 此处为读取文件的操作代码 */
	}
  
	fclose(fp);
}
