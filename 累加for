#include<stdio.h>
int main(int argc, char* argv[])
{
	int i, n, sum;
	printf("输入n");
	scanf("%d", &n);
	sum = 0;
	for(i = 1; i <= n; i ++)
	{
		sum = sum + i;
	 } 
	 printf("sum = %d\n",sum );
	 return 0;
}
